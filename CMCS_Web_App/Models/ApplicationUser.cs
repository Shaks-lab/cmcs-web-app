using Microsoft.AspNetCore.Identity;

namespace CMCS_ST10026321.Models
{
    public class ApplicationUser : IdentityUser
    {
        // Add extra custom properties later if needed (e.g. Department, FullName)
    }
}
